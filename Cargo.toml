[package]
name = "neon-context-server-extension"
version = "0.1.0"
edition = "2021"
description = "Zed extension wrapper for the Neon context server"
license = "MIT"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
zed_extension_api = "0.7.0"
serde = "1.0"
schemars = "0.8"
serde_json = "1"
